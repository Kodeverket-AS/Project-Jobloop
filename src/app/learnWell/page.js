import Image from "next/image";

export default function LearnWell() {
  return (
    <main className="flex flex-col items-center justify-between min-h-screen p-0">
      Velkommen til LearnWell, denne siden er under konstruksjon.
    </main>
  );
}